import { findDefaultExportIdentifierNames } from '../../src/config/loaders/ast';

describe('findDefaultExportIdentifierNames()', () => {
  it.each([
    ['', []],
    ['', []],
    ['', []],
    ['', []],
    ['', []],
  ])('should bla', () => {
    findDefaultExportIdentifierNames();
  });
});
